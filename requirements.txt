# Prefer `pip install -e .` (reads pyproject.toml). This file is provided as
# a convenience for pip-only workflows and CI.

typer>=0.12
httpx>=0.27
pydantic>=2.6
pydantic-settings>=2.2
rich>=13.7
python-dotenv>=1.0
imageio-ffmpeg>=0.5
faster-whisper>=1.1
